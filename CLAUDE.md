# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

Obsidian 스타일 주간보고 마크다운 파일을 Excel(XLSX) 파일로 변환하는 CLI 도구.
출력 형식은 `pointhub_weekly_report_sample_1.xlsx` 템플릿을 기준으로 한다.

## 실행

```bash
# 단일 파일 변환 (출력 파일명 자동: input.md → input.xlsx)
uv run python convert.py samples/sample.md

# 출력 경로 지정
uv run python convert.py weekly.md output/report.xlsx
```

## 아키텍처

```
convert.py          CLI 엔트리포인트 (click)
src/parser.py       MD → 데이터모델 파싱
src/exporter.py     데이터모델 → XLSX 생성
```

### 파서 (`src/parser.py`)

- `parse_document(text)` → `(doc_title, sections)`
- 체크박스: `[x]`=완료, `[ ]`=미완, `[-]`=취소, `[/]`=처리
- `⏱ Xh/Xh (담당자)` 형식에서 시간/담당자 자동 추출
- 담당자 추출은 **`⏱` 있는 항목** 또는 **줄 끝 짧은 한국어 이름 `(2~4자)` 패턴**에만 적용 (긴 설명문 오파싱 방지)
- 보조 함수: `extract_date_range`, `extract_assignees`, `extract_vacations`

### 익스포터 (`src/exporter.py`)

출력 시트 2개:

| 시트 | 열 구성 |
|------|---------|
| `개발파트` | A(spacer) · B(대분류) · C(중분류) · D(소분류) · E(기간) · F(작업자) · G(업무리스트) · H(진행률) · I(금주수행업무) · J(진행률) · K(비고) · L(업무지시) |
| `포인트허브_이슈사항` | A(부문) · B(항목) · C(담당자) · D(금주이슈) · E(차주이슈) · F(비고) |

MD 섹션 → XLSX 매핑:
- G열(업무리스트): `이번 주 목표` + `완료 업무` + `진행 중 업무` + `다음 주 예정`
- I열(금주수행업무): `업무 수행 내역` (요일별)
- D열(금주이슈): `특이사항/이슈`
- E열(차주이슈): `다음 주 예정`
- 휴가: `업무 수행 내역`에서 `[-] 이름 휴가` 항목 파싱

## 입력 MD 형식

```
# YYYY년 MM월 N주 주간 보고
## 📌 이번 주 목표          → 체크리스트
## 📋 업무 수행 내역         → ### 요일 (MM/DD) 서브섹션 + 체크리스트
## ✅ 완료 업무              → 목록 (담당자 괄호)
## 🔄 진행 중 업무
## ⏭️ 다음 주 예정
## 📝 특이사항 / 이슈        → ### / #### 중첩 서브섹션
## 📊 주간 회고              → **항목**: 값
```

## 분류 태그

업무 항목에 태그를 붙이면 XLSX 소분류 행과 셀 내 하위 항목으로 자동 분류됩니다.

| 태그 | 소분류 행 | 셀 내 하위 항목 |
|------|---------|--------------|
| *(없음)* | 포인트허브 개발 | 일반 개발 항목 |
| `#보안` | 포인트허브 개발 | 보안 및 인프라 |
| `#배포` | 운영 및 장애 대응 | 배포 및 모니터링 |
| `#버그` | 운영 및 장애 대응 | 버그 수정 |
| `#db` | 운영 및 장애 대응 | DB 및 서버 관리 |
| `#기타` | 기타 | — |

기존 `#todo`, `#important` 태그는 분류에 관여하지 않음.

## XLSX 개발파트 시트 구조

| 행 | 중분류 | 소분류 | G열 출처 | I열 출처 |
|----|-------|-------|---------|---------|
| 3 | 개발 | 포인트허브 개발 | 이번 주 목표 + 완료 업무 | 업무 수행 내역 |
| 4 | ↑ | 운영 및 장애 대응 | ← 태그 기반 필터 | ← 태그 기반 필터 |
| 5 | ↑ | 기타 | `#기타` 항목 | `#기타` 항목 |
| 6 | AO운영 | 정기 업무 | 고정 텍스트 | 고정 텍스트 |
| 7 | ↑ | 비정기 업무 | (빈 행) | (빈 행) |
